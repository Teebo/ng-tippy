import { TippyDirective } from './Tippy.directive';

describe('TippyDirective', () => {
  it('should create an instance', () => {
    const directive = new TippyDirective();
    expect(directive).toBeTruthy();
  });
});
